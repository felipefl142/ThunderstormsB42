-- ISUIElement.lua stub for testing
-- This is a minimal mock that satisfies require "ISUI/ISUIElement"

-- ISUIElement is already provided by PZMock and installed globally
-- This file just needs to exist so the require doesn't fail

-- Return empty table (ISUIElement is already set globally by PZMock)
return {}
